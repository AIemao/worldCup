/**
 * Constantes de rota — re-exporta de config/constants para uso interno
 * nos módulos de routing sem depender diretamente de @/config.
 */
export { ROUTES } from "@/config/constants";
export type { AppRoute } from "@/config/constants";
